spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url:  jdbc:mysql://localhost:3306/banking?characterEncoding=UTF-8&characterSetResults=UTF-8&useSSL=false
    username: root
    password: 32518458
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      format_sql: true
      show_sql: true
      hibernate:
        use_sql_comments: true

    show-sql: true
    database: mysql
  config:
    activate:
      on-profile: local

logging.level:
  #    org.hibernate.sql: debug
  org.hibernate.sql: info
  org.hibernate.jdbc.bind: trace

